import { DecodeStream } from "./DecodeStream.js";
import { Base } from "./Base.js";
//#region node_modules/restructure/src/Number.js
var NumberT = class extends Base {
	constructor(type, endian = "BE") {
		super();
		this.type = type;
		this.endian = endian;
		this.fn = this.type;
		if (this.type[this.type.length - 1] !== "8") this.fn += this.endian;
	}
	size() {
		return DecodeStream.TYPES[this.type];
	}
	decode(stream) {
		return stream[`read${this.fn}`]();
	}
	encode(stream, val) {
		return stream[`write${this.fn}`](val);
	}
};
var uint8 = new NumberT("UInt8");
var uint16be = new NumberT("UInt16", "BE");
var uint16 = uint16be;
var uint16le = new NumberT("UInt16", "LE");
var uint24be = new NumberT("UInt24", "BE");
var uint24 = uint24be;
new NumberT("UInt24", "LE");
var uint32be = new NumberT("UInt32", "BE");
var uint32 = uint32be;
var uint32le = new NumberT("UInt32", "LE");
var int8 = new NumberT("Int8");
var int16be = new NumberT("Int16", "BE");
var int16 = int16be;
new NumberT("Int16", "LE");
new NumberT("Int24", "BE");
new NumberT("Int24", "LE");
var int32be = new NumberT("Int32", "BE");
var int32 = int32be;
new NumberT("Int32", "LE");
new NumberT("Float", "BE");
new NumberT("Float", "LE");
new NumberT("Double", "BE");
new NumberT("Double", "LE");
var Fixed = class extends NumberT {
	constructor(size, endian, fracBits = size >> 1) {
		super(`Int${size}`, endian);
		this._point = 1 << fracBits;
	}
	decode(stream) {
		return super.decode(stream) / this._point;
	}
	encode(stream, val) {
		return super.encode(stream, val * this._point | 0);
	}
};
var fixed16be = new Fixed(16, "BE");
var fixed16 = fixed16be;
new Fixed(16, "LE");
var fixed32be = new Fixed(32, "BE");
var fixed32 = fixed32be;
new Fixed(32, "LE");
//#endregion
export { Fixed, NumberT as Number, fixed16, fixed16be, fixed32, fixed32be, int16, int16be, int32, int32be, int8, uint16, uint16be, uint16le, uint24, uint24be, uint32, uint32be, uint32le, uint8 };
