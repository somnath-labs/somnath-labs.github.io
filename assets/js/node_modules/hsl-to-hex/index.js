import { __commonJSMin } from "../../_virtual/_rolldown/runtime.js";
import { require_converter } from "../hsl-to-rgb-for-reals/converter.js";
//#region node_modules/hsl-to-hex/index.js
var require_hsl_to_hex = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var toRgb = require_converter();
	function max(val, n) {
		return val > n ? n : val;
	}
	function min(val, n) {
		return val < n ? n : val;
	}
	function cycle(val) {
		val = max(val, 1e7);
		val = min(val, -1e7);
		while (val < 0) val += 360;
		while (val > 359) val -= 360;
		return val;
	}
	function hsl(hue, saturation, luminosity) {
		hue = cycle(hue);
		saturation = min(max(saturation, 100), 0);
		luminosity = min(max(luminosity, 100), 0);
		saturation /= 100;
		luminosity /= 100;
		return "#" + toRgb(hue, saturation, luminosity).map(function(n) {
			return (256 + n).toString(16).substr(-2);
		}).join("");
	}
	module.exports = hsl;
}));
//#endregion
export default require_hsl_to_hex();
export { require_hsl_to_hex };
