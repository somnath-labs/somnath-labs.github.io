import { __toESM } from "../../../../_virtual/_rolldown/runtime.js";
import { require_react } from "../../../react/index.js";
import { require_jsx_runtime } from "../../../react/jsx-runtime.js";
import { Canvas, Checkbox, Circle, ClipPath, Defs, Document, Ellipse, FieldSet, Image, ImageBackground, Line, LinearGradient, Link, List, Marker, Note, Page, Path, Polygon, Polyline, RadialGradient, Rect, Select, Stop, Text, TextInput, TextInstance, Tspan, View, lib_exports } from "../../primitives/lib/index.js";
import { require_queue } from "../../../queue/index.js";
import PDFDocument from "../../pdfkit/lib/pdfkit.browser.js";
import FontStore from "../../font/lib/index.browser.js";
import { upperFirst } from "../../fns/lib/index.js";
import render$1 from "../../render/lib/index.js";
import layout from "../../layout/lib/index.js";
import renderer$1 from "../../reconciler/lib/index.js";
//#region node_modules/@react-pdf/renderer/lib/react-pdf.browser.js
var import_queue = /* @__PURE__ */ __toESM(require_queue(), 1);
var import_react = /* @__PURE__ */ __toESM(require_react(), 1);
var import_jsx_runtime = require_jsx_runtime();
var omitNils = (object) => Object.fromEntries(Object.entries(object).filter((_ref) => {
	let [, value] = _ref;
	return value !== void 0;
}));
var createInstance = (type, _ref) => {
	let { style, children, ...props } = _ref;
	return {
		type,
		box: {},
		style: style || {},
		props: props || {},
		children: []
	};
};
var createTextInstance = (text) => ({
	type: "TEXT_INSTANCE",
	value: text
});
var appendChild = (parent, child) => {
	const isParentText = parent.type === "TEXT" || parent.type === "LINK" || parent.type === "TSPAN" || parent.type === "NOTE";
	if (child.type === "TEXT_INSTANCE" && !isParentText) {
		console.warn(`Invalid '${child.value}' string child outside <Text> component`);
		return;
	}
	parent.children.push(child);
};
var appendChildToContainer = (parentInstance, child) => {
	if (parentInstance.type === "ROOT") parentInstance.document = child;
	else appendChild(parentInstance, child);
};
var insertBefore = (parentInstance, child, beforeChild) => {
	var _parentInstance$child;
	const index = (_parentInstance$child = parentInstance.children) === null || _parentInstance$child === void 0 ? void 0 : _parentInstance$child.indexOf(beforeChild);
	if (index === void 0) return;
	if (index !== -1 && child) parentInstance.children.splice(index, 0, child);
};
var removeChild = (parentInstance, child) => {
	var _parentInstance$child2;
	const index = (_parentInstance$child2 = parentInstance.children) === null || _parentInstance$child2 === void 0 ? void 0 : _parentInstance$child2.indexOf(child);
	if (index === void 0) return;
	if (index !== -1) parentInstance.children.splice(index, 1);
};
var removeChildFromContainer = (parentInstance, child) => {
	var _parentInstance$child3;
	const index = (_parentInstance$child3 = parentInstance.children) === null || _parentInstance$child3 === void 0 ? void 0 : _parentInstance$child3.indexOf(child);
	if (index === void 0) return;
	if (index !== -1) parentInstance.children.splice(index, 1);
};
var commitTextUpdate = (textInstance, oldText, newText) => {
	textInstance.value = newText;
};
var commitUpdate = (instance, updatePayload, type, oldProps, newProps) => {
	const { style, ...props } = newProps;
	instance.props = props;
	instance.style = style;
};
var createRenderer = (_ref2) => {
	let { onChange = () => {} } = _ref2;
	return renderer$1({
		appendChild,
		appendChildToContainer,
		commitTextUpdate,
		commitUpdate,
		createInstance,
		createTextInstance,
		insertBefore,
		removeChild,
		removeChildFromContainer,
		resetAfterCommit: onChange
	});
};
var { version } = { version: "4.6.0" };
var fontStore = new FontStore();
var renderer;
var events = {};
var pdf = (initialValue) => {
	const onChange = () => {
		var _events$change;
		const listeners = ((_events$change = events.change) === null || _events$change === void 0 ? void 0 : _events$change.slice()) || [];
		for (let i = 0; i < listeners.length; i += 1) listeners[i]();
	};
	const container = {
		type: "ROOT",
		document: null
	};
	renderer = renderer || createRenderer({ onChange });
	const mountNode = renderer.createContainer(container);
	const updateContainer = (doc, callback) => {
		renderer.updateContainer(doc, mountNode, null, callback);
	};
	if (initialValue) updateContainer(initialValue);
	const render = async function(compress) {
		if (compress === void 0) compress = true;
		const { pdfVersion, language, pageLayout, pageMode, title, author, subject, keywords, creator = "react-pdf", producer = "react-pdf", creationDate = /* @__PURE__ */ new Date(), modificationDate, ownerPassword, userPassword, permissions } = container.document.props || {};
		const ctx = new PDFDocument({
			compress,
			pdfVersion,
			lang: language,
			displayTitle: true,
			autoFirstPage: false,
			ownerPassword,
			userPassword,
			permissions,
			pageLayout,
			info: omitNils({
				Title: title,
				Author: author,
				Subject: subject,
				Keywords: keywords,
				Creator: creator,
				Producer: producer,
				CreationDate: creationDate,
				ModificationDate: modificationDate
			})
		});
		if (pageMode) ctx._root.data.PageMode = upperFirst(pageMode);
		const layout$1 = await layout(container.document, fontStore);
		return {
			layout: layout$1,
			fileStream: render$1(ctx, layout$1)
		};
	};
	const callOnRender = function(params) {
		if (params === void 0) params = {};
		if (container.document.props.onRender) container.document.props.onRender(params);
	};
	const toBlob = async () => {
		const chunks = [];
		const { layout: _INTERNAL__LAYOUT__DATA_, fileStream: instance } = await render();
		return new Promise((resolve, reject) => {
			instance.on("data", (chunk) => {
				chunks.push(chunk instanceof Uint8Array ? chunk : new Uint8Array(chunk));
			});
			instance.on("end", () => {
				try {
					const blob = new Blob(chunks, { type: "application/pdf" });
					callOnRender({
						blob,
						_INTERNAL__LAYOUT__DATA_
					});
					resolve(blob);
				} catch (error) {
					reject(error);
				}
			});
		});
	};
	const toBuffer = async () => {
		const { layout: _INTERNAL__LAYOUT__DATA_, fileStream } = await render();
		callOnRender({ _INTERNAL__LAYOUT__DATA_ });
		return fileStream;
	};
	const toString = async () => {
		let result = "";
		const { fileStream: instance } = await render(false);
		return new Promise((resolve, reject) => {
			try {
				instance.on("data", (buffer) => {
					result += buffer;
				});
				instance.on("end", () => {
					callOnRender();
					resolve(result);
				});
			} catch (error) {
				reject(error);
			}
		});
	};
	const on = (event, listener) => {
		if (!events[event]) events[event] = [];
		events[event].push(listener);
	};
	const removeListener = (event, listener) => {
		if (!events[event]) return;
		const idx = events[event].indexOf(listener);
		if (idx > -1) events[event].splice(idx, 1);
	};
	return {
		on,
		container,
		toBlob,
		toBuffer,
		toString,
		removeListener,
		updateContainer
	};
};
var Font = fontStore;
var StyleSheet = { create: (s) => s };
/**
* PDF hook
*
* @param {Object} [options] hook options
* @returns {[Object, Function]} pdf state and update function
*/
var usePDF = function(_temp) {
	let { document } = _temp === void 0 ? {} : _temp;
	const pdfInstance = (0, import_react.useRef)(null);
	const [state, setState] = (0, import_react.useState)({
		url: null,
		blob: null,
		error: null,
		loading: !!document
	});
	(0, import_react.useEffect)(() => {
		const renderQueue = (0, import_queue.default)({
			autostart: true,
			concurrency: 1
		});
		const queueDocumentRender = () => {
			setState((prev) => ({
				...prev,
				loading: true
			}));
			renderQueue.splice(0, renderQueue.length, () => state.error ? Promise.resolve() : pdfInstance.current.toBlob());
		};
		const onRenderFailed = (error) => {
			console.error(error);
			setState((prev) => ({
				...prev,
				loading: false,
				error
			}));
		};
		const onRenderSuccessful = (blob) => {
			setState({
				blob,
				error: null,
				loading: false,
				url: URL.createObjectURL(blob)
			});
		};
		pdfInstance.current = pdf();
		pdfInstance.current.on("change", queueDocumentRender);
		if (document) pdfInstance.current.updateContainer(document);
		renderQueue.on("error", onRenderFailed);
		renderQueue.on("success", onRenderSuccessful);
		return () => {
			renderQueue.end();
			pdfInstance.current.removeListener("change", queueDocumentRender);
		};
	}, []);
	(0, import_react.useEffect)(() => {
		return () => {
			if (state.url) URL.revokeObjectURL(state.url);
		};
	}, [state.url]);
	return [state, (0, import_react.useCallback)((newDoc) => {
		pdfInstance.current.updateContainer(newDoc);
	}, [])];
};
var PDFViewer = (_ref) => {
	let { title, style, className, children, innerRef, showToolbar = true, ...props } = _ref;
	const [instance, updateInstance] = usePDF();
	(0, import_react.useEffect)(() => updateInstance(children), [children]);
	const src = instance.url ? `${instance.url}#toolbar=${showToolbar ? 1 : 0}` : null;
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)("iframe", {
		src,
		title,
		ref: innerRef,
		style,
		className,
		...props
	});
};
var BlobProvider = (_ref) => {
	let { document: doc, children } = _ref;
	const [instance, updateInstance] = usePDF();
	(0, import_react.useEffect)(() => updateInstance(doc), [doc]);
	if (!doc) {
		console.warn("You should pass a valid document to BlobProvider");
		return null;
	}
	return children(instance);
};
var PDFDownloadLinkBase = (_ref, ref) => {
	let { fileName = "document.pdf", document: doc, children, onClick, href, ...rest } = _ref;
	const [instance, updateInstance] = usePDF();
	(0, import_react.useEffect)(() => updateInstance(doc), [doc]);
	if (!doc) {
		console.warn("You should pass a valid document to PDFDownloadLink");
		return null;
	}
	const handleDownloadIE = () => {
		if (instance && window.navigator.msSaveBlob) window.navigator.msSaveBlob(instance.blob, fileName);
	};
	const handleClick = (event) => {
		handleDownloadIE();
		if (typeof onClick === "function") onClick(event, instance);
	};
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)("a", {
		href: instance.url,
		download: fileName,
		onClick: handleClick,
		ref,
		...rest,
		children: typeof children === "function" ? children(instance) : children
	});
};
var PDFDownloadLink = /*#__PURE__*/ (0, import_react.forwardRef)(PDFDownloadLinkBase);
var throwEnvironmentError = (name) => {
	throw new Error(`${name} is a Node specific API. You're either using this method in a browser, or your bundler is not loading react-pdf from the appropriate web build.`);
};
var renderToStream = () => {
	throwEnvironmentError("renderToStream");
};
var renderToString = () => {
	throwEnvironmentError("renderToString");
};
var renderToFile = () => {
	throwEnvironmentError("renderToFile");
};
var render = () => {
	throwEnvironmentError("render");
};
({ ...lib_exports });
//#endregion
export { BlobProvider, Canvas, Checkbox, Circle, ClipPath, Defs, Document, Ellipse, FieldSet, Font, Image, ImageBackground, Line, LinearGradient, Link, List, Marker, Note, PDFDownloadLink, PDFViewer, Page, Path, Polygon, Polyline, RadialGradient, Rect, Select, Stop, StyleSheet, Text, TextInput, TextInstance, Tspan, View, createRenderer, pdf, render, renderToFile, renderToStream, renderToString, usePDF, version };
