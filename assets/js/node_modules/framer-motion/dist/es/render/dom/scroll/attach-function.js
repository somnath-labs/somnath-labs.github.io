import { observeTimeline } from "../../../../../../motion-dom/dist/es/scroll/observe.js";
import { scrollInfo } from "./track.js";
import { getTimeline } from "./utils/get-timeline.js";
import { isElementTracking } from "./utils/is-element-tracking.js";
//#region node_modules/framer-motion/dist/es/render/dom/scroll/attach-function.mjs
/**
* If the onScroll function has two arguments, it's expecting
* more specific information about the scroll from scrollInfo.
*/
function isOnScrollWithInfo(onScroll) {
	return onScroll.length === 2;
}
function attachToFunction(onScroll, options) {
	if (isOnScrollWithInfo(onScroll) || isElementTracking(options)) return scrollInfo((info) => {
		onScroll(info[options.axis].progress, info);
	}, options);
	else return observeTimeline(onScroll, getTimeline(options));
}
//#endregion
export { attachToFunction };
