import { isVariantLabel } from "../../../../../motion-dom/dist/es/render/utils/is-variant-label.js";
import { isControllingVariants } from "../../../../../motion-dom/dist/es/render/utils/is-controlling-variants.js";
//#region node_modules/framer-motion/dist/es/context/MotionContext/utils.mjs
function getCurrentTreeVariants(props, context) {
	if (isControllingVariants(props)) {
		const { initial, animate } = props;
		return {
			initial: initial === false || isVariantLabel(initial) ? initial : void 0,
			animate: isVariantLabel(animate) ? animate : void 0
		};
	}
	return props.inherit !== false ? context : {};
}
//#endregion
export { getCurrentTreeVariants };
