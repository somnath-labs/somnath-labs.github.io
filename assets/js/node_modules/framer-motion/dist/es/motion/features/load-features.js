import { setFeatureDefinitions } from "../../../../../motion-dom/dist/es/render/VisualElement.js";
import { getInitializedFeatureDefinitions } from "./definitions.js";
//#region node_modules/framer-motion/dist/es/motion/features/load-features.mjs
function loadFeatures(features) {
	const featureDefinitions = getInitializedFeatureDefinitions();
	for (const key in features) featureDefinitions[key] = {
		...featureDefinitions[key],
		...features[key]
	};
	setFeatureDefinitions(featureDefinitions);
}
//#endregion
export { loadFeatures };
